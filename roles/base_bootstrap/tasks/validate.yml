# Bootstrap role: validation and verification tasks

- name: "Validate | Collect Admin and Sudo Group Entries"
  ansible.builtin.getent:
    database: "{{ item.database }}"
    key: "{{ item.key }}"
  loop:
    - database: passwd
      key: "{{ base_bootstrap_user }}"
    - database: group
      key: "{{ base_bootstrap_sudo_group }}"
  loop_control:
    label: "{{ item.database }}"
  register: base_bootstrap_lookup

- name: "Validate | Read Admin Authorized_keys Only When Key Is Configured"
  ansible.builtin.slurp:
    path: "{{ base_bootstrap_lookup.results[0].ansible_facts.getent_passwd[base_bootstrap_user][4] }}/.ssh/authorized_keys"
  register: base_bootstrap_authorized_key_file
  changed_when: false
  failed_when: false
  when: base_bootstrap_authorized_key is defined and base_bootstrap_authorized_key | length > 0

- name: "Validate | Verify Admin Account Matches Configured Bootstrap Settings"
  ansible.builtin.assert:
    that:
      - base_bootstrap_user in base_bootstrap_lookup.results[0].ansible_facts.getent_passwd
      - base_bootstrap_lookup.results[0].ansible_facts.getent_passwd[base_bootstrap_user][1] | int == base_bootstrap_puid
      - base_bootstrap_lookup.results[0].ansible_facts.getent_passwd[base_bootstrap_user][2] | int == base_bootstrap_pgid
      - base_bootstrap_lookup.results[0].ansible_facts.getent_passwd[base_bootstrap_user][5] == base_bootstrap_user_shell
      - base_bootstrap_user in (
          base_bootstrap_lookup.results[1].ansible_facts.getent_group[base_bootstrap_sudo_group]
          | last
          | default('')
          | string
          | split(',')
          | map('trim')
          | reject('equalto', '')
          | list
        )
      - (
          base_bootstrap_authorized_key in (
            base_bootstrap_authorized_key_file.content
            | default('')
            | b64decode
            | split('\n')
            | map('trim')
            | reject('equalto', '')
            | list
          )
          if base_bootstrap_authorized_key is defined and base_bootstrap_authorized_key | length > 0
          else true
        )
    fail_msg: >-
      Validate | Admin account is not in the expected final state (uid/gid/shell/sudo group/authorized key)
    quiet: true
