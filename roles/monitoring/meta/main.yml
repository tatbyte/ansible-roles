---
# roles/monitoring/meta/main.yml
# Role metadata for `monitoring`.
# Declares dependency roles that provide the monitoring-related configuration.

dependencies:
  - role: monitoring_authorized_key
    tags: [monitoring, monitoring_authorized_key]
