---
# roles/base_needrestart/tasks/main.yml
# Task entrypoint for the `base_needrestart` role.
# Imports the assert, install, config, and validate phase files in order.

- name: Assert
  ansible.builtin.import_tasks: assert.yml
  tags: [assert, base_needrestart, base_needrestart_assert]

- name: Install
  ansible.builtin.import_tasks: install.yml
  tags: [install, base_needrestart, base_needrestart_install]

- name: Config
  ansible.builtin.import_tasks: config.yml
  tags: [config, base_needrestart, base_needrestart_config]

- name: Validate
  ansible.builtin.import_tasks: validate.yml
  tags: [validate, base_needrestart, base_needrestart_validate]
